import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.creditcard.manager',
  appName: 'Credit Card Manager',
  webDir: 'dist/credit-card-app/browser'
};

export default config;
